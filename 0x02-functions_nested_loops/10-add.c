#include "main.h"
/**
 * add - adds two number
 * @x: operand to be added
 * @y: operand to be added
 * Return: integer value
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	_putchar(sum);
	return (0);
}
