#include "main.h"
/**
 * _strcmp - compares two strings
 * @s1: the first string parameter
 * @s2: the second string parameter
 * Return: 0 if the two strings are the same another integers if not
 */
int _strcmp(char *s1, char *s2)
{

